services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: authsmith
      POSTGRES_PASSWORD: authsmith
      POSTGRES_DB: authsmith
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
